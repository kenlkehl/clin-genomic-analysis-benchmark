## The question that was asked

> {{ question_text }}

## Our list: the {{ gold_concepts | length }} thing(s) that needed pinning down

{% for c in gold_concepts -%}
{{ loop.index }}. {{ c }}
{% endfor %}

## The agent's list: what it said needed pinning down ({{ agent_concepts | length }} item(s))

{% for c in agent_concepts -%}
- {{ c }}
{% endfor %}

---

For each of the {{ gold_concepts | length }} concept(s) on our list, in order: does the
agent's list address that core concept at all? Answer `yes`, `no`, or
`unable to determine`, with your one-sentence reasoning written first.

Reply with valid JSON only.
