# Review for question {{ question.id }}  (cohort: {{ cohort.name }}, category {{ question.category }})

## Question text
> {{ question.text }}

## Analysis spec
- Population unit: **{{ question.analysis_spec.population_unit }}**
- Tables: {{ question.analysis_spec.tables | join(", ") }}
- Filters: {% if question.analysis_spec.filters %}{{ question.analysis_spec.filters | tojson }}{% else %}(none){% endif %}
- Statistic: {{ question.analysis_spec.statistic }}
- Expected answer type: **`{{ question.analysis_spec.expected_answer_type }}`**
{% if question.analysis_spec.notes %}- Notes: {{ question.analysis_spec.notes }}{% endif %}

## Cohort file inventory
{% for cat, names in files_by_category.items() %}{% if names %}- **{{ cat }}**: {{ names | join(", ") }}
{% endif %}{% endfor %}

## Required answer-type schema (`{{ question.analysis_spec.expected_answer_type }}`)
{{ answer_schema_md }}

## Candidate script
```python
{{ script }}
```

Decide whether this script faithfully implements the analysis spec. Respond with valid JSON only.
