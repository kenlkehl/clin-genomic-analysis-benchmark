# Repair attempt for question {{ question.id }}

The previous attempt failed. You must produce a corrected `def analyze(cohort_dir: Path) -> dict:`.

## Question
> {{ question.text }}

## Expected answer type
`{{ question.analysis_spec.expected_answer_type }}`

## Failure mode
{{ failure_mode }}

## Failing script body (previous attempt)
```python
{{ previous_body }}
```

## Failure output
```
{{ failure_output | truncate(4000) }}
```

## Validation errors (if any)
{% if validation_errors %}{% for e in validation_errors %}- {{ e }}
{% endfor %}{% else %}(no validation errors — the script failed before producing a result){% endif %}

## Reviewer feedback (if any)
{% if reviewer_feedback %}{{ reviewer_feedback }}{% endif %}

Reply with a single corrected fenced ```python``` block. The function signature MUST be `def analyze(cohort_dir: Path) -> dict:`. Same constraints as before.
