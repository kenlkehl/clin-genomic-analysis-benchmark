"""Agent contract + harness orchestration.

The harness is model- and framework-agnostic. Agents are CLI executables that
match the contract in `contract.py`:

    $ <agent_cmd> --question-file <question.json> --output <result.json>
"""
